#include <SFML/Graphics.hpp>
#include <SFML/Audio.hpp>
#include "globals.h"

class Ship {
    public:
        Ship(sf::Color color, float initialX, float initialY);
        void draw();

    private:
};
